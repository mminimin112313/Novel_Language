# Common Checklist

- Compiler has zero `error` diagnostics.
- Plot validation logs are reproducible.
- Tests pass (`npm run test`).
- No new untracked runtime artifacts are committed.
- Docs paths match actual folder locations.
